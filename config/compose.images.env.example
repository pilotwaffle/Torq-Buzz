# Digests must be resolved by Resolve-ComposeImages.ps1 before any pull (C5).
# Leave empty under C1 (no Docker mutation authorized).
POSTGRES_IMAGE=postgres:17
REDIS_IMAGE=redis:7
MINIO_IMAGE=minio/minio:latest
